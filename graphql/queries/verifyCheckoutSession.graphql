query GetCheckoutSessionStatus($sessionId: String!) {
  getCheckoutSessionStatus(sessionId: $sessionId) {
    errors {
      field
      message
    }
    reservationIds
    payment_status
    sessionUrl
  }
}
