mutation ConfirmConnectedAccount($wineryAlias: String!) {
  confirmConnectedAccount(wineryAlias: $wineryAlias) {
    errors {
      field
      message
    }
    winery {
      ...WineryFragment
    }
  }
}
